# 리턴봇(ReturnBot) 시뮬레이션 프로젝트 작업 명세 — 한국 아파트 환경 기준

> Claude Code 작업 지시서. 한국 아파트 공용부(복도·엘리베이터홀)를 기준 환경으로 삼아, 분리수거함 회수 로봇의 URDF 모델링 → ROS 2 내비게이션 → Isaac Sim 디지털 트윈 검증까지 단계별로 구축한다.
> 각 Phase는 독립적으로 완료 검증 후 진행. 불명확한 수치는 임의 확정하지 말고 `TODO(확인)` 주석으로 남기고 질문할 것.

---

## 1. 프로젝트 개요

- **목적**: 아파트 복도를 주행하며 세대 앞 분리수거함을 회수·교환하는 자율주행 로봇의 주행 스택을 하드웨어 조립 전에 시뮬레이션으로 검증
- **핵심 검증 항목**
  1. 한국 아파트 복도 유형별(편복도/중복도/계단식 홀) 차체 치수·회전 여유 → 프레임 치수 확정
  2. Nav2 파라미터 사전 튜닝 (실기 이식 시 무수정 목표)
  3. 문턱·경사로 통과, 피벗턴 시 캐스터 저항에 의한 경로 밀림 재현
  4. 세대문 앞 정차·AprilTag 도킹 정렬 (Phase 4)
- **원칙**: 시뮬 검증한 ROS 2 노드·설정을 실기(라즈베리파이 5 + ESP32)에 그대로 이식. 시뮬 전용/공용 코드는 패키지 수준 분리.

## 2. 기준 환경 — 한국 아파트 공용부 (핵심 섹션)

기성 한국 아파트 시뮬 에셋은 없으므로 **파라메트릭 자작을 기본**으로 하고, 소품·포토리얼은 후속 단계에서 외부 에셋을 얹는다.

### 2.1 아파트 유형 3종 (모두 파라메트릭 생성)

| 유형 | 특징 | 시뮬 포인트 |
|---|---|---|
| A. 편복도(갓복도)형 | 한쪽 벽면에 세대문 일렬, 유효폭 1.2 m 기준(법규 최소) | 최악 조건. 회전·교행 여유 검증의 기준 월드 |
| B. 중복도형 | 양쪽 세대문, 유효폭 1.8 m 기준 | 마주보는 세대문 동시 배출 시나리오 |
| C. 계단식(홀형) | 엘리베이터홀 + 세대 2~4호 조합, 복도 없음 | 홀 내 다점 정차·회전, 최신 아파트 주류 |

- 치수는 전부 xacro/USD 변수로: 복도 유효폭(1.2/1.5/1.8 m), 층고, 세대문 폭 0.9 m `TODO(확인)`, 문턱 높이 5~20 mm 가변
- 근거 문서: 주택건설기준 등에 관한 규정의 복도 폭 조항, LH 공공주택 표준설계도(공개 자료) 수치를 `docs/env_reference.md`에 출처와 함께 정리하는 작업을 Phase 0에 포함

### 2.2 필수 장애물·구조물 (한국 특화)

- 세대문 앞: 소화전함(벽 돌출), 우유함, 세대 앞 적치물(유모차·자전거 — 동적/정적 랜덤 배치 옵션)
- 엘리베이터홀: 승강기 문(폭 0.9 m 기준 `TODO(확인)`), 홀 내 기둥, 방화문(상시 열림/닫힘 두 상태)
- 바닥: 복도 타일(마찰 높음) / 에폭시 / 경사로 8% / 문턱 15 mm 기본
- 조명: 주간 자연광 + 야간 복도등(센서등 점멸 포함 — Phase 4 카메라 검증용)

### 2.3 에셋 조달 전략 (Phase별)

1. **Phase 0~3**: 파라메트릭 자작 지오메트리만 사용. 텍스처 최소화, 콜리전 정확도 우선
2. **Phase 4**: AI허브 실내 3D 데이터셋(가상 실내 공간 3D 합성 데이터, 실내 공간 3D 종합 데이터)에서 한국식 소품 fbx 추출 → USD 변환 스크립트 작성. 라이선스 조건 `docs/env_reference.md`에 기록
3. **실증 단지 확정 후**: 폰 LiDAR/포토그래메트리 스캔 → 메시 정리 → USD 임포트 파이프라인 (기존 사내 아이작심 스캔 워크플로 재사용). 3DGS 기반 상용 에셋은 카메라 합성데이터 단계에서만 검토

## 3. 대상 하드웨어 사양 (시뮬 모델 기준값)

| 항목 | 값 | 비고 |
|---|---|---|
| 구동 방식 | 차동구동 2륜 + 캐스터 2 | 조향기구 없음 |
| 총 중량 | 60 kg | 자중+적재 포함 |
| 구동륜 | 지름 200 mm(8인치), 폭 50 mm | 허브모터, 솔리드타이어 |
| 바퀴 간격 L | 450 mm (가안) | 편복도 1.2 m 검증 결과로 확정 |
| 차체 외형 | 550(W)×700(D)×400(H) mm (가안) | 1.2 m 복도 교행 불가 전제, 회전 여유 우선 |
| 캐스터 | 지름 100 mm, 후방 | 스위블 |
| 속도 | 최고 1.0 / 운용 0.6 m/s | 공용부 보행자 혼재 전제 |
| 무게배분 | 구동축:캐스터 = 60:40 | |
| LiDAR | LD19: 360°, 12 m, 10 Hz | 차체 상부 중앙 |
| IMU | 6축 (BNO085 기준) | |
| 카메라 | 전방 1개 (AprilTag) | Phase 4 |
| 컴퓨팅 | 실기: Pi 5(ROS 2) + ESP32(UART) | 시뮬은 PC 대체 |

## 4. 기술 스택

- ROS 2 Humble, CycloneDDS 고정
- Phase 1~3: Gazebo(gz sim) / Phase 4: Isaac Sim + ROS 2 Bridge, RTX LiDAR
- slam_toolbox(맵핑 1회) → AMCL + Nav2, colcon, rclpy 우선

## 5. 저장소 구조

```
returnbot_ws/src/
├── returnbot_description/   # URDF(xacro), 메시
├── returnbot_env/           # ★ 한국 아파트 파라메트릭 월드 생성기 (Gazebo world + USD 공용)
├── returnbot_bringup/       # 공용 launch·파라미터
├── returnbot_sim/           # 시뮬 전용 launch
├── returnbot_navigation/    # Nav2 config, 맵, BT
└── returnbot_docking/       # Phase 4
```

## 6. 작업 단계

### Phase 0 — 환경 기준 정리 (신규)
- [ ] `docs/env_reference.md`: 복도 폭 법규 기준, LH 표준설계도 공개 자료에서 편복도/중복도/홀형 대표 치수 발췌(출처 명기), 불확실 수치는 TODO 표기
- [ ] `returnbot_env`에 파라메트릭 월드 생성기 골격: 유형(A/B/C)·폭·문턱·장애물 밀도를 인자로 받아 Gazebo world(sdf)와 USD를 동일 소스에서 생성
- **완료 기준**: 인자만 바꿔 3유형 월드가 생성되고 스크린샷이 docs에 저장됨

### Phase 1 — URDF 모델링
- [ ] xacro: base_link, 구동륜 2(continuous), 캐스터 2(저마찰 구형 근사, 주석 명시), lidar/imu/camera_link
- [ ] 관성값은 xacro 매크로 계산(60 kg, 60:40 배분), 치수 전부 인자화
- [ ] robot_state_publisher + RViz2 launch
- **완료 기준**: check_urdf 통과, TF 정상, 바퀴간격·차체폭 인자 노출

### Phase 2 — Gazebo 주행 검증
- [ ] diff_drive·LiDAR·IMU 플러그인, /cmd_vel /scan /odom 표준 네이밍
- [ ] Phase 0 생성기로 월드 3종 × 문턱 15 mm × 경사로 8% 구성
- [ ] 텔레옵 주행 → slam_toolbox 맵 저장 (유형별 3개 맵 커밋)
- **완료 기준**: 3유형 주행·맵핑 정상

### Phase 3 — Nav2 자율주행
- [ ] AMCL + Nav2 (Pi 5 성능 고려: controller 20 Hz, costmap 0.05 m 시작점)
- [ ] 미션: 엘리베이터홀 출발 → 세대문 앞 순차 정차 3초(문 앞 0.3 m, 문과 평행 자세) → 복귀
- [ ] rotate-in-place 후 출발 정책, 피벗턴 경로 밀림 측정 스크립트
- [ ] 매트릭스 측정: 차체폭 550/600/650 × 유형 A(1.2 m)/B(1.8 m)/C(홀) → 통과·미션 성공률 md 리포트 자동 생성
- [ ] 세대 앞 적치물 랜덤 배치 시나리오에서 회피·미션 유지율 측정
- **완료 기준**: 유형 A 1.2 m에서 왕복 미션 성공률 95%+, 매트릭스 리포트 산출

### Phase 4 — Isaac Sim 트윈 + 도킹
- [ ] Phase 0 생성기의 USD 출력을 Isaac에 로드, ROS 2 Bridge로 Phase 3 스택 무수정 연결, RTX LiDAR를 LD19 스펙으로
- [ ] AI허브 소품 fbx→USD 변환 스크립트 + 소화전·우유함·적치물 배치
- [ ] AprilTag(tag36h11) 수거함 + 접근 정렬 노드 (정지 오차 ±30 mm, ±3°), 야간 복도등·센서등 조명 조건별 성공률
- [ ] 시연 영상 녹화 스크립트 (입대의·지원사업 발표용 앵글 프리셋 포함)
- **완료 기준**: Gazebo 동일 미션 재현 + 도킹 성공률 리포트

## 7. 공통 규칙

- 파라미터 전부 yaml, 매직넘버 금지. 실기/시뮬 분기는 launch 인자로만
- Phase 완료 시 `docs/phase{N}_report.md` 기록, 커밋은 작업 단위, 한글 메시지 허용
- 측정 코드는 표준 토픽만 구독 (실기 재사용 전제)

## 8. 알려진 리스크

- 캐스터 스위블 저항은 Gazebo 근사가 약함 → Phase 4에서 조인트 기반 캐스터 상세화 검토
- 솔리드타이어 마찰, 문턱 충격은 시뮬-실기 갭 존재. 시뮬 값은 튜닝 시작점
- 문턱 통과 실패 시 바퀴 상향(10인치) 전에 접근 속도·진입 각도부터 검토
- LH 표준설계도 수치와 실증 단지 실측이 다를 수 있음 → 파라메트릭 유지가 보험
